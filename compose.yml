services:
  copyparty:
    image: copyparty
    container_name: copyparty
    ports:
      - "3923:3923"
    volumes:
      - /home/chyld/data/copyparty:/partyhome
      - /mnt/truenas:/truenas
    restart: unless-stopped

